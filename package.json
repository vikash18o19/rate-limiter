{
  "name": "rate-limiter",
  "version": "1.0.0",
  "description": "The rate-limiter package helps developers limit the number of requests made to an API within a givin time frame.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "vikash",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0"
  }
}
